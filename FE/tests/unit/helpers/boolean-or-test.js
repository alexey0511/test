import { booleanOr } from '../../../helpers/boolean-or';
import { module, test } from 'qunit';

module('Unit | Helper | boolean or');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = booleanOr(42);
  assert.ok(result);
});
